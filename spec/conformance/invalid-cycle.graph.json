{
  "apiVersion": "graphengineering.reacher-z.github.io/v1alpha1",
  "kind": "Graph",
  "metadata": { "name": "invalid-cycle", "version": "1.0.0" },
  "inputSchema": { "type": "object" },
  "outputSchema": { "type": "object" },
  "entrypoints": ["one"],
  "outputs": { "result": { "node": "two" } },
  "nodes": [
    { "id": "one", "kind": "transform", "inputSchema": {}, "outputSchema": {}, "config": {} },
    { "id": "two", "kind": "transform", "inputSchema": {}, "outputSchema": {}, "config": {} }
  ],
  "edges": [
    { "id": "one-two", "from": { "node": "one" }, "to": { "node": "two" } },
    { "id": "two-one", "from": { "node": "two" }, "to": { "node": "one" } }
  ]
}
