/** Minimum Node.js version supported by the built-in SQLite adapter. */
export const SQLITE_CYCLE_STORE_MINIMUM_NODE_VERSION = "22.16.0" as const;

/** Stable provider identity shared by the TypeScript and Python adapters. */
export const SQLITE_CYCLE_STORE_PROVIDER_ID = "sqlite-local" as const;
