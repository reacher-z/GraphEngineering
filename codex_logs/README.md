# Codex development log

This directory records evidence-backed progress for the Graph Engineering build.
Raw run, agent, scan, and nudge records stay local and are ignored by Git. Sanitized
daily summaries, architecture decisions, and incident reports may be committed.

Events are append-only and must never contain credentials, authorization headers,
private user data, or full model prompts/responses.
