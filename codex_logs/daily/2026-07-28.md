# Graph Engineering development log — 2026-07-28

## Full-plan audit and source milestone

Two read-only agents completed independent reads of the entire master plan.
Authoritative status remains incomplete: 107 registry tasks are 41 completed,
7 in progress, and 59 planned; release evidence has no selected candidate and
weight 0/93; all 178 mapped release leaves remain Open; active SQLite/runtime
schema is v1 and the exact 96 campaign reports `implementationClaim:false`.
The public repository has not achieved the requested adoption/star outcome.
No plan-complete, stable-release, or popularity claim was made.

Committed and pushed `293aa2727bb987c315b54dc6f5dcb447e770bdb2` as
`reacher-z <mtrxcop@gmail.com>` with no coauthor. It added cross-language
stream-head/record-identity capture, canonical record/value carrier auditing,
an exclusive Python owner capability, comment/semicolon-safe transaction
epochs, count-preserving DML rejection, and bounded one-row record reads.
Remote and local branch SHA were identical after push. Final evidence was
SQLite 119 tests, ledger contract 14 tests, Python 1,181 tests, build/lint/type
checks, Ruff, and strict MyPy. Four hostile review passes ended with no local
HIGH/MEDIUM under the internal source-only contract.

## Checkpoint carriers

Implemented the next two baseline families in parallel across TypeScript and
Python: checkpoint-current and checkpoint-revision. Both use canonical key
ordering, shared checkpoint and ledger codecs, exact byte re-encoding, scalar
value support, hash/length/outer identity binding, and transaction/count
guards. Current rows are fetched one at a time. Revisions emit canonical
numeric key order `1,10,2`; put requires six payload fields and delete requires
all six explicitly null.

Hostile coverage includes current value/checkpoint/summary mutations, revision
summary/outer drift, incomplete puts, partial deletes behind ignored CHECK
constraints, and scalar values. Current verification: TypeScript SQLite 119
tests plus typecheck/lint/build; Python baseline/source 36 tests plus Ruff and
strict MyPy. Read-only TS audit reports no implementation HIGH/MEDIUM.

The five remaining source families, all cross-family relation anti-joins,
cursor seal, FILE-backed staging, 100K proof, atomic v2 migration/publication,
request/sequence replay, crash matrix, and exact 96 implementation switch
remain open.

A Python hostile pass then caught revision `fetchmany(256)`, which could
materialize over 256 MiB of valid summary carriers. It was changed to
`fetchmany(1)` and protected by an exact family fetch-sequence spy. Independent
re-review and 36 focused tests closed the finding with HIGH 0 / MEDIUM 0.

## Twelve-family source closure

Completed the four remaining scalar identity families and the final
legacy-operation carrier in TypeScript and Python. The source iterator now
covers all twelve frozen baseline kinds in exact rank/key order. Lease state,
used lease/lock fences, holds, 9 closed mutation result shapes, the explicit
16 MiB legacy result limit, byte-identical codec re-encoding, logical result
hash, independent raw BLOB SHA-256, one-row large-carrier reads, count guards,
and non-leaking fixed errors are covered.

Verification: TypeScript SQLite 14 files / 121 tests, typecheck, lint, and
build; Python source 39 tests, Ruff, and strict MyPy over implementation and
tests. A child Agent completed the full Python suite at 1,206 passes. The
ledger contract remained 14/14 and explicitly reported
`implementationClaim:false`. Independent TS audits reported HIGH 0 / MEDIUM 0.
A Python audit found three strict typing errors in newly added tests; the
fixtures and casts were narrowed to `CycleStoreProviderOperation`, after which
strict MyPy, 39 tests, and Ruff passed.

The master plan was append-only extended with the exact non-claim boundary and
the next FILE-backed TEMP stage, controlled-write guard, normalized relations,
anti-join rule registry, cursor seal, disposal, hostile matrix, 100K evidence,
and atomic migration handoff. Runtime v2, relation reconciliation, cursor
rebind, 100K evidence, crash closure, request/sequence/replay, exact-96
implementation evidence, release, and adoption outcomes remain open.

## Reconciliation registry and owner/TEMP preparation

Added a closed reconciliation registry with 53 ordered/phase-bound `BLR_*`
rules, executable safe diagnostics, FILE TEMP defaults/bounds, cursor seal
domains, explicit deferred root status, and all release/throughput/
implementation claims false. Added a normative reconciliation document and
linked it from the protocol index. Registry hostile tests, the complete
19-test ledger contract, and 286 documentation links pass.

Implemented internal TypeScript and Python FILE-backed TEMP configuration plus
owner-observed EXCLUSIVE transaction proof. Both runtimes use an 8 MiB default
and 1..64 MiB bounds, read back FILE/cache/spill, forbid every tested route to
`temp_store_directory`, reject configuration in a transaction, and reject
deferred/immediate/unknown capture.

Hostile audits found transaction-swap proof retention, later-statement and
quoted directory PRAGMA bypasses, Python 32 MiB protocol drift, missing cache
bounds, qualified spill behavior, unexplained DDL, and an `ANALYZE` epoch gap.
All were repaired. TS structured error code/operation tests were added. Python
now rejects capture after TEMP DDL or either ANALYZE form and locks down
savepoint-only plus failing executescript behavior.

Current evidence: TypeScript SQLite 128 tests plus typecheck/lint/build;
registry 5 and ledger contract 19; Python focused source/stage 46 plus Ruff and
strict MyPy. Full Python was 1,211 passes immediately before the final two
ANALYZE parameter cases and is queued for final rerun. No relation table,
cursor root, 0002 execution, runtime-v2 publication, 100K claim, or release
claim was made.

Final owner audit then found a TypeScript prepared-DDL epoch bypass and
transaction-scoped TEMP readback. Prepared schema/PRAGMA execution is now
epoch-tracked across `all/get/iterate/run`; capture-after-prepared-TEMP-DDL is
hostile-tested; active-transaction readback is rejected; commented EXCLUSIVE
BEGIN and savepoint parity are covered. Final evidence is TS 129 tests and
Python 1,213 full tests, with all static, contract, registry, and docs gates
green. Relation staging and every later non-claim remain open.

## FILE-backed common stage and normalized relation catalog

Implemented the transaction-bound TEMP catalog foundation in TypeScript and
Python. The stage now requires the bounded FILE/cache/spill profile and the
exact current owner EXCLUSIVE transaction; reserves a case-insensitive
`ge_blr_*` namespace; creates one common stage, twelve normalized
STRICT/WITHOUT ROWID carrier tables, ten indexes, and a twelve-rank relation
key view; and verifies exact object identity plus table flags before use.

Common writes revalidate kind-specific key/state bytes and shared identity,
require statement and total-change deltas of exactly one, maintain grouped
counts, and poison permanently on duplicate, semantic drift, or unexplained
writes. Carrier DDL closes schema clocks/version bounds, stream tail sentinel,
record predecessor, checkpoint put/delete, lease/lock active identity,
used-epoch fence, and legacy nine-operation nullable-group smuggling attacks.

Hostile review found and closed cross-runtime DDL drift, a stale-stage
replacement deletion, partial cleanup error masking, missing Python grouped
and bidirectional coverage assertions, silent Python cleanup residue,
unconfigured TEMP acceptance, TS same-count catalog substitution, DML during
catalog creation, generic wrong-kind canonical bytes, repeated-create PRAGMA
epoch invalidation, and uppercase reserved-name bypass. Disposal now either
proves complete reverse cleanup or reports poison, while stale ownership never
deletes caller replacement objects.

Final evidence: TypeScript SQLite 15 files / 144 tests, typecheck, lint, and
build; Python 1,237 full tests and 28 focused stage tests, Ruff, and strict MyPy
over changed implementation/tests;
SQLite ledger/reconciliation/migration contract 19 tests; scoped diff check
clean. Production relation decoders/writers, real non-test paired +2 writes,
source consumer, relational rules, cursor root/seal, 0002, permanent
publication, 100K, crash/replay, release, throughput, and popularity claims
remain open. The final independent review reported HIGH 0 / MEDIUM 0 / LOW 0.

## Twelve-family paired relation writers

Implemented internal paired common/relation writers in TypeScript and Python.
All twelve baseline kinds now use fixed statements, canonical recapture,
statement-local `changes == 1`, total-change `+1` per half and exact `+2` per
pair. Relation failure after a successful common write permanently poisons the
stage and requires caller rollback; no compensating delete or retry exists.

Legacy loading uses one main-qualified source lookup before either write and
proves metadata, 2..16 MiB raw bounds, raw SHA-256, codec decode/re-encode and
logical result hash. Both runtimes project all nine result shapes into the
closed 38-column row/30-column discriminator, reject nonzero sub-millisecond
precision, and survive a hostile TEMP table shadow. Final-read DML injection
is now caught by both common-count and bidirectional coverage barriers.

Evidence: TypeScript SQLite 15 files / 155 tests, focused stage 33, typecheck,
lint and build; Python 1,263 full tests, focused stage 54, Ruff and strict MyPy;
ledger/reconciliation/migration contract 19; docs links 286; diff check clean.
Final parity audit reported HIGH 0 / MEDIUM 0 / LOW 0.

The production source iterator is intentionally not integrated: its frozen
`total_changes` guard rejects the owned TEMP `+2` on resume. The next slice is
an unforgeable cooperative receipt/allowance contract, not a global guard
relaxation or an in-memory collection. Source-to-stage loading, relational
rules, cursor seal/root, 0002, publication, scale, crash/replay, release and
popularity claims remain open.

## Cooperative source-to-stage streaming

Implemented the private cooperative bridge from the real twelve-family v1
source iterator to the paired FILE-backed TEMP stage in TypeScript and Python.
The enforced lifecycle is TEMP configuration, `BEGIN EXCLUSIVE`, stage
creation, source capture and one-current-item streaming. Each opaque
single-use receipt binds the exact connection, transaction epoch, item, stage,
sequence, before count and exact `+2` after count. The source advances its
private allowance only after immediate receipt consumption and an independent
write-counter fence; the public iterator's frozen `total_changes` behavior is
unchanged.

The reconciler keeps no entry collection, permits no standalone/cooperative
lane mixing, finalizes active statements on every exit, preserves the first
authoritative failure through cleanup, and finishes with exact common counts
and bidirectional relation coverage. External DML is tested before common,
between common/relation, after relation/before receipt, during validation and
between fetches. Missing, forged, replayed, cross-stage and field-mutated
receipts; rollback/rebegin; savepoints; prepared/trusted DDL; legacy mutation;
relation failure after exact common `+1`; early close; and cleanup-error
masking are hostile-tested. Exact real 12-kind and 1,024-entry mixed streams
produce 24 and 2,048 TEMP changes respectively.

Independent review found and closed two TypeScript HIGH issues involving
primary-error replacement and `Generator.return()` control flow, then found
and closed a Python MEDIUM lane-mixing issue and a cleanup-primary hardening
gap. Final cross-runtime review is HIGH 0 / MEDIUM 0 / LOW 0.

Final evidence: TypeScript SQLite 16 files / 172 tests with 17 focused
reconciliation tests, typecheck, lint and build; Python 1,289 full tests with
26 focused cooperation and 122 combined source/stage/cooperation tests, Ruff
and strict MyPy; ledger/reconciliation/migration contract 19; documentation
links 286; scoped diff checks clean. `implementationClaim` remains false.
There is still no 0002 execution, permanent v2 write, invariant campaign,
ordered TEMP handoff/cursor root, 100K proof, crash/replay closure, release or
popularity claim.

## Verified ordered TEMP handoff

Implemented the next internal reconciliation stage in both runtimes: a
one-shot reader over the verified TEMP common table ordered by numeric kind
rank and binary key bytes. Each row is fetched singly, fenced before/after,
rank/kind checked, canonically decoded/re-encoded, byte-compared and streamed
into the existing constant-memory accumulator. The handoff retains only one
row, one preceding sort key and twelve integer counts, then returns only the
frozen projection identity after EOF, exact per-kind counts, common/relation
coverage, catalog shape, root seal and a post-root fence.

Exact source-summary/stage/session/connection/epoch/count binding prevents a
same-shape summary or partially completed cooperative stream from acquiring
the reader. One-shot, early-close, rollback/rebegin, savepoint, catalog
replacement, missing/extra/reordered/duplicate/kind-swapped/noncanonical rows,
real and injected DML at every fetch/root/completion boundary, active cursor
disposal and cleanup failure are hostile-tested. Row and catalog cursor close
failures surface when no primary exists and never replace an authoritative
primary error.

Both runtimes match their materializing builder at exact 12 and 1,024 rows and
share one literal pristine three-entry projection golden. Independent reviews
found and closed missing Python exact-summary binding, consumed per-kind
counters, premature completion and swallowed cleanup failures. Final review is
HIGH 0 / MEDIUM 0 / LOW 0.

Evidence: TypeScript SQLite 199 tests with 44 focused handoff/cooperation,
typecheck/lint/build; Python 1,317 full tests with 28 focused handoff and 150
combined chain, full Ruff and strict MyPy; contract 19; docs 286; diff checks
clean. There is still no relational invariant execution, persistent root,
cursor seal, migration 0002 execution, runtime-v2 publication, 100K proof,
crash/replay closure, release or popularity claim.

## Stream/record relational invariant campaign

Implemented the first registered relational campaign over the verified
FILE-backed TEMP projection in TypeScript and Python. The private one-shot
runner executes seven fixed, bounded and named-index-audited queries for
record/stream ownership, persisted empty streams, sequence gaps, predecessor
links, exact final tails, tenant/hash duplication and canonical common/relation
binding. Added the distinct `BLR_RECORD_BINDING` rule, bringing the frozen
registry to 54 rules while keeping `implementationClaim:false`.

The campaign is bound to the exact sealed projection object, owner connection,
EXCLUSIVE epoch, session, write counter, twelve-kind counts and exact TEMP
catalog. Every query lifecycle boundary is fenced; active cursors are
stage-owned; terminal completion re-proves total/per-kind counts,
bidirectional key coverage and catalog shape. Reports contain only the frozen
projection identity and safe three-field capped diagnostics. Closed option
validation does not evaluate hostile accessors.

Independent audit found and closed shallow query freezing, closed-options
bypasses, overbroad predecessor SQL, stale-tail false negatives, missing
terminal per-kind proof, cleanup lifecycle gaps and initially unequal
cross-runtime fixtures. The final shared 21-entry fixture has literal identity
`fa4f/f061/f121/8ad` and exact diagnostic counts `1/2/2/1/3`; it covers a
foreign-tenant alias, persisted empty streams, first/interior gaps,
predecessor drift, stale-behind/missing/wrong tails and exact plan usage.

Final evidence: TypeScript SQLite 16 files / 232 tests, focused reconciliation
77, typecheck/lint/build; Python full 1,356, focused campaign 39, related chain
204, Ruff/strict MyPy; SQLite contract 19 with registry ruleCount 54; docs 286;
diff checks clean. Final independent review: HIGH 0 / MEDIUM 0 / LOW 0.

The append-only master plan now records checkpoint §31.34.30 and the detailed
checkpoint relational campaign gate §31.34.31. No checkpoint/lease/lock/hold /
legacy campaign, cursor seal, permanent root, `0002`, runtime-v2 publication,
100K, crash/replay, release or popularity claim was made.

## Checkpoint relational invariant campaign

Three parallel lanes implemented, adversarially reviewed and independently
verified the private TypeScript/Python checkpoint campaign. The six fixed
rules cover revision gaps, current/put record orphans, latest put/delete
current semantics, stale current rows and bidirectional rank-four/rank-five
binding. The stage requires the exact completed stream/record predecessor and
re-proves projection, source summary, connection, exclusive epoch, write
counter, per-kind counts, common/relation coverage and exact TEMP catalog.
Every marker cursor boundary is fenced and cleanup keeps the primary failure.

The shared 43-entry hostile fixture uses capture time `1785110405000`, literal
identity suffixes `fa4f/f061/c458/264a` and diagnostic vector `2/3/2/2/1/1`.
Direct minimal SQL tests cover all nested summary fields, delete JSON null,
clock-only drift, zero-history current and tenant/scope/checkpoint partitioning.
Common-only and relation-only equal-count attacks are rejected at the stronger
campaign-begin coverage fence and never admitted into a partial report.

Main-thread final gates: TypeScript checkpoint 45/45 and SQLite 277/277 with
typecheck, lint and build green; Python checkpoint 57/57, adjacent baseline
261/261 and full 1,413/1,413 with Ruff and strict MyPy green; SQLite contract
19/19, registry 54 rules, documentation 286 links and `git diff --check`
green. The independent final review reported HIGH 0 / MEDIUM 0 / LOW 0.

The master plan was append-only extended with verified checkpoint §31.34.32
and detailed lease/migration-lock/legal-hold gate §31.34.33. No lease/lock /
hold, legacy, cursor, clock, migration `0002`, permanent-v2, 100K, crash,
release, adoption or star completion claim was made.

## Lease, migration-lock and legal-hold invariant campaign

Three parallel lanes implemented and adversarially reviewed the next private
SQLite campaign in TypeScript and Python. Six token-identical marker-only SQL
rules now prove exact stream ownership for leases/holds, complete lease and
migration-lock epoch histories, active terminal identity/clock binding and
bidirectional canonical common/relation fields for ranks six through ten. The
campaign is one-shot after checkpoint completion and retains the exact owner,
projection, EXCLUSIVE epoch, source/count/coverage/catalog and cursor cleanup
fences.

Query-plan review removed `count(DISTINCT)`, unbounded derived-history
materialization and automatic indexes. Correlated aggregates use the named
epoch indexes; lock rules have no TEMP sort and lease history retains only one
FILE-backed outer group for tenant/stream diagnostic-unit normalization.

The shared 46-entry fixture has literal identity suffixes
`fa4f/f061/6b9290/ebc3` and vector `1/3/2/1/1/1`. It includes valid zero,
retired and active controls plus orphan, start/interior/extra history, wrong
active ID/clock, lock and hold attacks. Direct production-SQL tests cover all
ten key fields and 37 state fields, active null tuples, clocks, expiry,
high-water, versions, physical UNIQUE/PK rejection and table/index replacement.

Main-thread final evidence: TypeScript focused 56/56 and SQLite 333/333 with
typecheck/lint/build green; Python focused 138/138, adjacent 399/399 and full
1,551/1,551, with Ruff and strict MyPy including the new test green after a
small annotation/import-order correction; contract 19/19, registry 54 rules,
docs 286 links and scoped diff checks green. Final independent review reports
HIGH 0 / MEDIUM 0 / LOW 0.

The append-only plan now records verified §31.34.34 and detailed legacy gate
§31.34.35. No legacy/cursor/clock implementation, migration `0002`, permanent
v2 write, persistent seal/root, 100K, crash/replay, release, adoption or star
completion claim was made.

## Lease/lock/hold evidence wording correction

Final read-only integration audit found two documentation-only MEDIUM wording
issues and no runtime defect. The master plan now append-only clarifies that
cursor creation is parameterized over all six rules while fetch/close mutation
uses the shared loop at representative rules (TypeScript also parameterizes
fetch). It also clarifies that `BLR_STREAM_EMPTY` is a report-and-continue
diagnostic: later lease/hold rules see the stream row and correctly avoid a
duplicate missing-stream report. After correction the final audit is HIGH 0 /
MEDIUM 0 / LOW 0.

## Final Python count correction

The final hostile-test additions raised Python full-suite evidence from the
earlier 1,551 snapshot to 1,568 passing tests plus two subtests. Focused remains
138/138 and adjacent baseline remains 399/399. The master plan records this as
an append-only correction; implementation and review severity are unchanged.

## Canonical Python count authority

Main-thread rerun of the exact repository gate `cd python && uv run pytest -q`
produced 1,551/1,551. The auxiliary 1,568-plus-subtests figure used a
noncanonical collection/accounting scope and is superseded. The append-only
master plan records 1,551 as the sole authoritative full-suite count; focused
138 and adjacent 399 are unchanged.

## Legacy recoverable-binding invariant campaign

Completed the private TypeScript/Python legacy-operation recovery gate after
ordered handoff, stream/record, checkpoint and lease/lock/hold verification.
Five fixed marker-only rules cover bidirectional inventory plus the strongest
recoverable append, checkpoint, lease and migration-lock facts for the closed
nine-operation mutation set. The implementation never reselects or decodes the
main `result_blob`, adds no public export and adds no permanent schema object.

The stage now captures and repeatedly re-proves the main operations catalog
receipt together with owner, EXCLUSIVE epoch, write counter, TEMP catalog,
counts, relation coverage and exact projection reference. Cursors are bounded
to `diagnosticLimit + 1`, streamed one row at a time and closed exactly once
with primary-failure precedence.

An external Claude Code read-only audit found and drove closure of two missed
release blockers: TypeScript lacked six safe-history non-overbinding cases, and
a relation key substitution could duplicate one inventory identity. The SQL
now keeps inventory branches mutually exclusive, both runtimes freeze the
substitution count at one, TS mirrors all six safe evolutions, and begin rejects
equal-count unexplained TEMP writes before rule preparation. Result-blob digest
and operation-name drift plus frozen required-index topology are also covered.

The shared pristine identity remains 20 entries / nine legacy operations with
projection `459ecad40c2ed54c59c38bb3fecbabfc71694bdf4f59f1eebbdae749fbb9dd62`.
The hostile identity remains 28/17 with projection
`a23da0ae704c5d1414fd55950ff1f306eeffb8d5408340dbe713e2fa02b72647`
and vector `(0, 2, 2, 3, 2)`. The final five normalized SQL hashes are:

1. inventory `47955bd3ba75cbfd515d95cff82dd28213d956818e06a597e07a7376a7874f46`;
2. append `c18ddc48822f41fa3e6dcafcbdfbb26aed83f9ebcb135c959d5bf32fd0c3b055`;
3. checkpoint `46a86173db67c4a6e573d68c5bfdf4a8b886862f16857a1c538cbe953ab1ca4d`;
4. lease `619daa1f02963c6a4ef58331db5157ed2f8a95aa0f99cc3add27e2063f541212`;
5. migration lock `14ac5f2c3bf01c7b3ac6744b209bded53e7e3001ab94084431a1b92f0f782247`.

Final main-thread gates: TypeScript SQLite 16 files / 387 tests plus
typecheck/lint/build; Python canonical full 1,640 tests plus Ruff and strict
MyPy. The master plan was append-only extended with the verified closure and a
corrected cursor/provider-clock design gate. Legacy 10K/100K performance,
cursor completion, migration `0002`, permanent v2 state, crash/replay, release,
adoption and 5K/6K stars remain explicitly unclaimed.

## Cursor immutable-seal A1 implementation

Claude Code was used as an actual coding lane, not only as a reviewer. In an
isolated worktree it produced the private TypeScript cursor physical decoder,
constant-space immutable-seal accumulator and 21-test adversarial suite. The
Python lane independently produced the equivalent runtime module and tests.
The main integration lane then reconciled both contracts after three read-only
cross-runtime reviews found source-identity, manual-carrier-validation,
one-shot-finish and receipt-naming differences.

The reconciled implementations consume the exact eighteen-column v1 cursor
shape, reduce both BLOBs to bounded length/digest evidence, validate all Slice
A1 physical/nullability/scope/clock invariants, stream rows in token-then-tenant
byte order and seal them with the frozen row/chain domains. Both bind the source
descriptor and schema identity beside the immutable root, revalidate every
carrier at append, reject atomicity/order/count violations and allow exactly
one successful finish. A shared TypeScript fixture is independently executed
by Python and freezes identical empty, event, checkpoint and pair roots.

Current focused evidence is TypeScript 21/21 and Python 31/31, with TypeScript
typecheck and Python Ruff/format/strict MyPy green. Full repository gates and a
fresh severity review are running before commit. The work remains a private A1
primitive: no database integration, source-clock capture, projection receipt,
TEMP campaign, cursor rebind, migration, persistent state, registry claim,
10K/100K performance or release/popularity claim is made.

Final reconciliation additionally changed the Python accumulator to accept the
complete decoded seal row, so source descriptor/schema identity is checked at
the same pre-state-advance boundary as TypeScript. Exact four-field receipt and
source-mismatch atomicity assertions close both residual LOW review notes. The
fresh independent audit reports HIGH 0 / MEDIUM 0 / LOW 0.

Final gates: TypeScript cursor 21/21 and SQLite 17 files / 408 tests, with
typecheck/lint/build; Python cursor 31/31, legacy 92/92 and full 1,674/1,674,
with full Ruff, strict MyPy over 48 source files and scoped formatting; contract
19/19, registry 54 with `implementationClaim:false`, docs 286 and diff checks
green. Plan/log changes remain append-only. Cursor A2/B, clocks, migration,
persistence, scale, crash/replay, release and popularity remain unclaimed.

## Full-plan audit and cursor source-clock A2a

Three parallel review lanes read the complete 10,372-line master plan and
audited complementary domains: TypeScript/product/runtime, Python/conformance/
testing, and SQLite/security/release. The main lane consolidated their evidence
into `codex_logs/reviews/MASTER-PLAN-COMPLETION-AUDIT-2026-07-28.md`. The plan is
materially incomplete: the task snapshot is 41 completed, seven in progress and
59 planned out of 107; the evidence scan is 13 satisfied and 64 open out of 77;
and 178 release-checklist leaves remain open. Cursor A2b/Slice B, integrated
router/barrier execution, production durability, native cycles, adapters,
budget enforcement, observability, Explorer, course/release/growth work remain
on the critical path.

Claude Code 2.1.220 with Opus was run as a real authoring lane in isolated
worktree `/home/nick/work/GraphEngineering-claude-clock` on branch
`feat/claude-clock-evidence`. It produced the initial TypeScript A2a source-clock
split and adversarial tests. The process was interrupted after its code was
complete but its interactive session stopped making progress; the main lane
inspected the complete diff and integrated it with `apply_patch`, preserving
review authority in the primary worktree.

The main lane implemented the Python equivalent and reconciled both runtimes on
the exact three-field clock envelope: capture clock, maximum non-cursor observed
clock and provider high-water. Cursor creation/consumption clocks and all expiry
or checkpoint RFC3339 values are excluded from the source maximum. Fifteen
non-cursor branches are retained. The normalized SQL hash is
`c85e9836aadf613752aa9ba078c00248a4aa5cc3faafda916409b1ddf3201e1b`.
Both runtimes reject unsafe, negative or fractional persisted values and enforce
`captured >= providerHighWater >= maximumNonCursorObserved`.

Independent final audits reported HIGH 0 / MEDIUM 0 / LOW 0 for TypeScript,
Python and cross-runtime parity. TypeScript focused is 13/13 and SQLite full is
17 files / 412 tests; typecheck, lint and build are green. Python focused
source/handoff/cursor is 106/106 and canonical full is 1,679/1,679; Ruff,
format and strict MyPy are green. Contract validation remains 19/19, registry
remains 54 with `implementationClaim:false`, conformance is green and no public
export was added.

The master plan was append-only extended with the completion truth, A2a
acceptance, exact A2b receipt sequence, twelve-step Slice B campaign queue and
the parallel D6 router/barrier lane. A2a alone issues no opaque pre-rebind
receipt, executes no SQL, creates no TEMP object, rebinds no cursor, runs no
migration and persists no v2 state. Performance, crash/replay, release,
adoption and 5K/6K-star claims remain explicitly open.

## Cursor A2b opaque ownership receipt

Completed the private TypeScript/Python A2b capability and receipt primitive.
Four module-minted ownership handles and one capture session now bind the exact
source summary, three-clock evidence, A1 cursor seal and actual baseline
projection into an opaque one-shot pre-rebind receipt. The package-private
receipt-only fence rejects clones, retains the complete exact object graph and
does not consume or transition lifecycle state.

The independent review rejected two protocol drafts before code acceptance.
The first confused main-table tenant/token order with future TEMP token/tenant
seal order and failed to bind the actual baseline projection. The second used a
fabricated projection digest. After implementation, review found a TypeScript
spread that leaked `nonceSha256` into a fourteenth receipt field; it was removed
because the capture-session root already commits the nonce. Both runtimes and a
third independent computation then converged on the exact thirteen-field bytes.

The real shared receipt roots are empty `e63c0eed...032ad`, one event
`cb710d7e...43f6`, event plus checkpoint `f0571ef8...6c7` and hostile
MAX_SAFE/1,024 fleet `e319abee...7fa`. Each runtime constructs a fresh SQLite
source, captures and accumulates its real three-entry projection, and recomputes
the A1 seal through production code before issuing A2b.

Claude Code was also launched in isolated worktree
`/home/nick/work/GraphEngineering-claude-a2b`. Its alternative code was rejected
and the process stopped because it retained the wrong query order, omitted the
actual projection identity and used reconstructable scalar ownership. No line
from that candidate was integrated.

Final evidence: TypeScript A2b 14/14, adjacent 280/280, SQLite 426/426 and
typecheck/lint/build green. Python A2b 25/25, adjacent 131/131, full 1,704/1,704
and Ruff/format/strict MyPy49 green. Independent audit is HIGH 0 / MEDIUM 0 /
LOW 0. A2b remains SQL/TEMP/rebind/migration/stage/registry-free; Slice B,
publication, post-rebind verification, scale, crash/replay, release, adoption
and star goals remain open.

## D6 conditional-routing runtime tranche

Implemented the first real scheduler-integrated D6 slice in TypeScript and
Python. Native routers now invoke the deterministic route-selection primitive,
execute only the fixed versioned `RouteEquals` edges, settle unselected paths as
zero-attempt `ROUTE_NOT_SELECTED`, propagate inactivity and bind merge nodes from
active inputs only. Custom route results are recomputed against the authoritative
bound input and compiled policy before any success journal event.

Durable start/resume now validates router input-policy-output identity,
unsupported-condition zero-attempt settlements, inactive branch history and
real interleaved scheduling order. Independent attack review found and closed
input substitution, invalid-decision retry, forged inactive execution, forged
unsupported-condition execution, crash/resume rejection, terminal projection
and scheduled-order defects. The final review is HIGH 0 / MEDIUM 0 / LOW 0.

Final gates: TypeScript runtime 243/243, focused 85/85, typecheck/lint/build;
patterns 93/93 and primitives 147/147. Python focused is 94/94 and the final
post-parity full rerun is 1,749/1,749 plus two nested subtests, with Ruff, format
and strict MyPy green. Docs links and diff checks
are green. Detailed TS, Python and cross-runtime audit reports are under
`codex_logs/reviews/` with the `D6-...-2026-07-28.md` names.

Claude Code 2.1.220 Opus ran in isolated worktree
`/home/nick/work/GraphEngineering-claude-d6`. Its contract draft changed the
existing carrier/config shapes and contained duplicate fields, so it was stopped
and none of its output was integrated.

The Slice B architecture lane also produced
`SQLITE-CURSOR-SLICE-B-DESIGN-AUDIT-2026-07-28.md`. A proposed A2b/TEMP ordering
change was challenged and withdrawn after exact source review proved A1 can be
computed before TEMP. The retained blocker is binding the DB-free A2b receipt to
the exact live captured-source connection immediately before TEMP creation.

This milestone does not complete D6. Compiler route exhaustiveness/default
diagnostics, a dedicated `RouteSelected` durable identity, shared conformance,
quorum/deadline barriers, trace/CLI visibility and full router/barrier replay
remain open. It also performs no Cursor Slice B SQL, TEMP creation, rebind,
migration or registry completion claim.

## Cursor Slice B0a captured-source connection fence

Completed and independently accepted the first pre-transfer ownership half of
Cursor Slice B B0 in TypeScript and Python. Both runtimes now verify the A2b
receipt first, derive the exact retained summary/clock only from its provenance,
require a genuinely module-captured summary, bind it to the exact live
EXCLUSIVE connection and capture epoch, and return a repeatably checked private
witness. No cursor SQL, cursor TEMP object, rebind, migration or permanent state
was added.

Attack review prevented two unsafe implementations from being committed. A
TypeScript subclass could previously hide a real epoch-tracked PRAGMA behind an
overridden getter; the fence accepted the old epoch while private state had
advanced. Owner observation now uses a captured base-class intrinsic backed by
private SQLiteConnection fields and two stable snapshots. Python's first frozen
witness could be cloned with `dataclasses.replace`; the accepted witness is now
exact-identity registered in a weak registry and rejects replace/copy/copied-
token/direct/subclass clones. Closed Python owners now fail with deterministic
owned errors while forged receipts still fail A2b first.

Both runtimes prove the real predecessor chain: baseline TEMP creation before
capture, cooperative handoff, ordered projection and clean stream/record,
checkpoint, lease/lock/hold and legacy campaigns. The transaction epoch stays
fixed while owner-authorized `total_changes` advances. B0a intentionally ignores
that historical counter; an unexplained later TEMP DML is rejected by the
adjacent stage fence.

Final evidence is TS focused 7/7 and SQLite 433/433 with typecheck/lint/build;
Python focused 13/13, adjacent 224/224 and full 1,762/1,762 plus two nested
subtests with Ruff/format/strict MyPy. Language-swapped final reviews are HIGH
0 / MEDIUM 0 / LOW 0. Claude Code/Opus supplied an additional read-only review
and no repository edits.

The next mandatory slice is B0b: a one-way exact source/receipt/projection/
stage/current-epoch/current-allowed-change transfer before the first cursor
TEMP DDL. B0a does not complete B0, Slice B, rules 1-10, rebind, migration,
release, adoption or star objectives.

The next B0b implementation is now backed by a separate 624-line closed-owner
bridge brief covering exact one-way epoch transfer, the first-DDL seam, mirrored
hostile tests and parallel delivery lanes; it remains design-only in this
milestone.

## D6 integrated-router compiler contract revision 2

Froze the corrected D6 compiler/runtime contract after rejecting the first
candidate at HIGH 3 / MEDIUM 4 / LOW 1. Revision 2 makes `routedBranches`
policy lowering and compiler enforcement atomic, preserves the published
`RouteEquals` carrier, registers the three existing loop conditions instead of
misclassifying them, and removes the nonexistent lower-level scheduler bypass.

The corpus now has 6 pattern, 29 policy, 14 condition, 2 JSON-envelope, 4 host-
capture, 18 compiler-graph and 11 reachable runtime cases over 7 graphs. Exact
canonical bytes and literal hashes match TypeScript/Python 25/25; runtime
projections match 11/11 per language. A final audit found and closed one more
ordering gap with deliberately non-lexical multi-router double-GE1401 and
double-GE1402 cases. Final hostile disposition is HIGH 0 / MEDIUM 0 / LOW 0.

This is contract freeze only. GE1401-GE1407 production compiler passes,
pattern source changes, durable RouteSelected identity, integrated barriers,
quorum/deadlines, trace and CLI remain open.

## Cursor Slice B0b/B1 accepted implementation

Completed and independently accepted the exact stage-owner handoff and first
cursor TEMP seal in TypeScript and Python. B0b now consumes B0a once, binds the
exact receipt/source/projection/connection/completed-stage tuple, burns one
private session and performs no SQL. B1 executes one constant 30-column STRICT
WITHOUT ROWID TEMP DDL, accepts only owner epoch `+1` and row changes `+0`,
validates the complete phase-aware catalog and adopts only the live stage
epoch. The historical capture epoch remains immutable.

The final audits closed dynamic hook replacement, Python DDL-close primary
error leakage, cleanup residue, secondary-over-primary exception replacement
and rollback/rebegin same-name replacement deletion. Cleanup now requires the
same EXCLUSIVE owner generation, unchanged row count, positive rootpage and
exact stored SQL, so a stale stage cannot delete a replacement object. Shared
fixture coverage locks nine top-level fields, 17 scenarios and all 30 xinfo
rows in both languages.

Final evidence: TS focused 31/31, SQLite 464/464 plus typecheck/lint/build;
Python focused 44/44, adjacent 609/609, isolated B0-only full 1,804 passed plus
two skipped/two subtests, and current combined full 1,815 plus two subtests;
Ruff/format/MyPy, docs, diff and package-root non-export checks green. Final
independent audit is HIGH 0 / MEDIUM 0 / LOW 0 in
`SQLITE-CURSOR-SLICE-B-B0B-B1-FINAL-CONSOLIDATED-AUDIT-2026-07-28.md`.

B2 rules 1-10, A1 production-root reproduction, cursor publication/rebind,
migration `0002`, v2 persistence, scale, replay, release, adoption and stars
remain open and are not claimed by this milestone.

## D6 integrated-router production compiler and runtime gates

Implemented and independently accepted the bounded integrated-router
production tranche in TypeScript and Python. Both compilers now enforce the
exact direct router policy, registered condition ownership, allowed routes,
duplicate cases/targets and exhaustive coverage through GE1401-GE1407, with
category order plus global edge declaration order. `routedBranches` lowers
legacy empty, explicit and preconfigured policies deterministically.

The frozen pattern matrix was corrected from the impossible `quick, audit`
normalization to the existing Unicode `audit, quick` order; all six cases are
now consumed under both input permutations. Ordinary and durable runtimes now
preflight compiler-valid foreign loop conditions across the complete graph and
fail with zero handlers/executors, zero attempts, empty nodes and zero history
read/append. Both language compilers consume all seven runtime hashes and the
hash contract; both public runtimes consume all 11 runtime cases literally.

Initial production audit H3/M1/L1 and a later README LOW were fully remediated.
Final gates are core 238, patterns 106, runtime 257, Python full 1,835 plus two
subtests, Ruff/format/MyPy54, fixtures73, docs286 and diff-check green. Final
independent result is H0/M0/L0 in
`D6-INTEGRATED-ROUTER-PRODUCTION-FINAL-AUDIT-2026-07-28.md`.

Dedicated durable route-decision identity and integrated barrier/quorum/
deadline/late-arrival/cancellation/trace/CLI work remain open; D6 overall is
not claimed complete.

## Cursor Slice B2 implementation in progress

Froze the cross-runtime B2 fixture for exact source/insert/seal/lookup/marker
SQL, five independently derived semantic roots, ten-rule order, diagnostic
bounds, closed outcomes, EQP predicates and conservative whole-protocol
nonclaims. The canonical fixture digest is
`6acb99d9593b37d6e29b6862531e3ade2dae051b21a628a26cc4e1d7e2fdb8bc`;
its validator and eight hostile contract tests are green. The master plan was
extended append-only, including an explicit distinction between representable
sanitized keys and physically nonstageable rows; no existing plan line was
changed.

TypeScript now has private tolerant inspection, exact one-shot stage ownership,
the ten-rule campaign, bounded diagnostics, closed EQP acceptance, source and
catalog fences, cancellation, A1 reproduction and clean/diagnosed terminal
states. Real tests currently cover empty, event, checkpoint/event cross-order,
all isolated rules, aggregate rule order, truncation, nonstageable evidence,
history mutation, stale authority and 128/1,024-row conflicting orders. The
latest main-thread focused run is 66/66 green; broader fault injection remains
in progress.

Python now has the mirrored private inspector/campaign, exact fixture SQL and
hash checks, closed catalog/EQP gates, physical TEMP count, owner-preserving
cursor cleanup, A1 reproduction and real empty/event/checkpoint/cross-order
tests. The rejected two-cursor ownership design was fully removed: EQP reuses
the sole registered campaign-cursor slot while synchronous point lookups retain
bounded local cleanup. The latest main-thread focused run is 86/86 with Ruff
lint/format and scoped strict MyPy green.

This is not a B2 acceptance record. Python cancellation-boundary injection,
the full Python ten-rule/limit/lifecycle/scale matrix, remaining TypeScript
fault injection, cross-runtime parity, complete suites and an independent
HIGH 0 / MEDIUM 0 / LOW 0 audit are still required before commit/push. The
30-minute `graph-progress.timer` remained active and completed its 19:41 PDT
scan; the next scan was scheduled for 20:11 PDT.

## Cursor Slice B2 adversarial remediation in progress

Ran a separate read-only Claude Code audit while the TypeScript, Python and
independent-review lanes continued in parallel. The audit reproduced a real
cross-runtime storage-class vector divergence, exact staged-tuple sentinel
differences, cancellation/catalog precedence inversions, marker-shape gaps,
post-prepare cursor ownership leaks and missing executable parity evidence.
None of those findings was treated as acceptance merely because the earlier
runtime-local suites were green.

The runtime lanes have since closed the canonical seven-object source-catalog
identity gate, exact lookup/marker value checks, catalog-before-cancellation
precedence, original insert-error identity, immediate cursor ownership after
successful prepare, Python row/carrier liveness, wrong-arity R7/R8 attribution,
column-11 storage R8-only attribution and the fresh/alpha-v0-to-v1 lineage
campaign path. Python also replaced repeated seven-row catalog reproof with a
bounded one-row scalar snapshot and added real increment/decrement resource
telemetry. Its 128-row characterization plus precedence group completed in
45.60 seconds; its 36-label cancellation group was green before the stricter
per-statement/per-rule label expansion now underway.

Added a real cross-runtime executable harness at
`tools/conformance/sqlite_cursor_pre_rebind_parity.mjs` with a Python evidence
peer. After the sentinel remediation, its `--allow-remaining` mode compares 23
exact TEMP stage tuples, five row digests, five pristine roots/outcomes and ten
isolated-plus-aggregate diagnostic vectors with zero TypeScript/Python drift.
The default strict gate intentionally remains red until all 12 pristine, 27
hostile and 31 lifecycle fixture obligations plus production campaign resource
evidence are consumed literally. This remains an implementation-progress
record, not a B2 completion, release, throughput, adoption or star claim.
